/** Pixel height of a single hour row in the calendar grid. */
export const HOUR_HEIGHT = 64

export const TOTAL_HOURS = 24

/** Smallest increment (in hours) tasks snap to when dragged or resized. */
export const SNAP_HOURS = 0.25

export const LOCAL_STORAGE_KEY = 'caler-storage'
